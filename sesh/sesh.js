seshType = ['joints','bongs','dabs', 'all']

var sesh = function(obj){
  this.card = obj.card,
  this.description = obj.description,
  this.img = obj.image,
  this.clickableMiddleCards = obj.clickableMiddleCards,
  this.clickableUserCards = obj.clickableUserCards
}
